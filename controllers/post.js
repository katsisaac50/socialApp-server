const Post = require('../models/post');
const User = require('../models/user');
const postByUser = async(req, res) => {
    console.log(req)

    try {
        
    } catch (error) {
        
    }
}

module.exports = {
    postByUser
}